<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
  </head>
  <body>
    <h1>{{ title }}</h1>
    <p>
      Please choose from the following quizzes.
      <ol>
        {{#quizzes}} <li><a href="{{address}}">{{quizName}}</a></li>
        {{/quizzes}}
      </ol>
    </p>
  </body>
</html>
